CREATE TABLE IF NOT EXISTS web_builder.sites (uuid UUID PRIMARY KEY, owner_uuid UUID NOT NULL, name VARCHAR(255) NOT NULL, domain VARCHAR(255) UNIQUE, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW());
