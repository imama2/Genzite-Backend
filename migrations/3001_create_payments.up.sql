CREATE TABLE IF NOT EXISTS payments.payments (uuid UUID PRIMARY KEY, payer_uuid UUID NOT NULL, amount NUMERIC(18,2) NOT NULL, currency VARCHAR(10) NOT NULL, status VARCHAR(50) NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW());
